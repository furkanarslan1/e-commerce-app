export const currenyUSD = new Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "USD",
});

export const currenyTRY = new Intl.NumberFormat("tr-TR", {
  style: "currency",
  currency: "TRY",
});
